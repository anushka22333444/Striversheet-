class Solution {
    public static int largest(int[] arr) {
        // Sort the array
        Arrays.sort(arr);
        
        // Return the last element (largest) in the sorted array
        return arr[arr.length - 1];
    }
}
